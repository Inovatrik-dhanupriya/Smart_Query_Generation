# If you start Streamlit from the repo root (e.g. `streamlit run nl_to_sql/ui/streamlit_app.py`).

[client]
showSidebarNavigation = false

[server]
# Avoid Windows watchdog permission errors on hot-reload.
fileWatcherType = "poll"

[theme]
primaryColor = "#5b21b6"
